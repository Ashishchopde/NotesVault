<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>About - NotesVault</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/style.css">
  <style>
    /* Navbar brand styles */
    .navbar-brand {
      display: flex;
      align-items: center;
      flex-direction: row-reverse;
    }

    .navbar-brand img {
      height: 40px;
      width: auto;
      margin-right: 0;
      margin-left: 0;
    }

    .brand-text {
      font-size: 1.5rem;
      font-weight: bold;
      color: white;
      text-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
      letter-spacing: 1px;
    }

    /* About section styles */
    .about-section {
      padding: 2rem 0;
    }

    .about-card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 15px;
      padding: 1.5rem;
      margin-bottom: 2rem;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .about-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    }

    .about-header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .about-header h1 {
      font-size: 2.2rem;
      font-weight: bold;
      color: white;
      margin-bottom: 0.8rem;
      text-shadow: 0 0 15px rgba(120, 115, 245, 0.5);
    }

    .about-header p {
      color: rgba(255, 255, 255, 0.8);
      font-size: 1rem;
    }

    .feature-icon {
      font-size: 2rem;
      margin-bottom: 1rem;
      color: #7873f5;
      text-shadow: 0 0 10px rgba(120, 115, 245, 0.5);
    }

    .team-section {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 15px;
      padding: 2rem;
      margin-top: 3rem;
    }

    .team-header {
      text-align: center;
      margin-bottom: 2rem;
      cursor: pointer;
      padding: 1rem;
      border-radius: 10px;
      transition: background-color 0.3s ease;
    }

    .team-header:hover {
      background: rgba(255, 255, 255, 0.1);
    }

    .team-content {
      display: none;
      padding: 1rem;
    }

    .team-content.active {
      display: block;
      animation: fadeIn 0.5s ease;
    }

    .team-member {
      padding: 0.5rem 1rem;
      border-radius: 8px;
      margin-bottom: 0.5rem;
      transition: transform 0.2s ease, background-color 0.2s ease;
      display: flex;
      align-items: center;
    }

    .team-member:hover {
      transform: translateX(10px);
      background: rgba(255, 255, 255, 0.1);
    }

    .team-member i {
      color: #7873f5;
      margin-right: 1rem;
    }

    .contact-section {
      text-align: center;
      margin-top: 3rem;
    }

    .contact-icon {
      font-size: 2rem;
      color: #7873f5;
      margin-bottom: 1rem;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .rotate-icon {
      transition: transform 0.3s ease;
    }

    .rotate-icon.active {
      transform: rotate(180deg);
    }

    .feature-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .feature-item {
      text-align: center;
      padding: 1.2rem;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 10px;
      transition: transform 0.3s ease;
    }

    .feature-item:hover {
      transform: translateY(-3px);
    }

    .feature-item h3 {
      font-size: 1.2rem;
      margin: 0.5rem 0;
      font-weight: 600;
    }

    .feature-item p {
      font-size: 0.9rem;
      color: rgba(255, 255, 255, 0.8);
      margin-bottom: 0;
    }
  </style>
</head>
<body>
  <!-- Starfield -->
  <canvas id="starfield"></canvas>

  <!-- Blob -->
  <div class="blob-bg">
    <div class="blob"></div>
  </div>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <div class="navbar-brand">
        <a href="/" class="navbar-item">
          NotesVault
        </a>
      </div>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <div class="search-container">
          <input type="text" class="search-input" id="searchInput" placeholder="Search documents...">
          <i class="fas fa-search search-icon"></i>
        </div>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="/"><i class="fas fa-home"></i> Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin"><i class="fas fa-user-shield"></i> Admin</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="/about"><i class="fas fa-info-circle"></i> About</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container about-section">
    <!-- About Header -->
    <div class="about-header">
      <h1>Welcome to NotesVault</h1>
      <p>Your secure platform for managing and accessing educational documents</p>
    </div>

    <!-- Features Grid -->
    <div class="about-card">
      <div class="feature-grid">
        <div class="feature-item">
          <i class="fas fa-shield-alt feature-icon"></i>
          <h3>Secure Storage</h3>
          <p>Your documents are stored with enterprise-level security</p>
        </div>
        <div class="feature-item">
          <i class="fas fa-search feature-icon"></i>
          <h3>Smart Search</h3>
          <p>Find documents quickly with our powerful search feature</p>
        </div>
        <div class="feature-item">
          <i class="fas fa-file-pdf feature-icon"></i>
          <h3>Format Support</h3>
          <p>Support for DOCX and PDF file formats</p>
        </div>
        <div class="feature-item">
          <i class="fas fa-mobile-alt feature-icon"></i>
          <h3>Responsive Design</h3>
          <p>Access your documents on any device</p>
        </div>
      </div>
    </div>

    <!-- Team Section -->
    <div class="about-card team-section">
      <div class="team-header" id="teamHeader">
        <h2>Meet Our Team <i class="fas fa-chevron-down rotate-icon"></i></h2>
        <p>The minds behind NotesVault</p>
      </div>
      <div class="team-content" id="teamContent">
        <div class="text-center mb-4">
          <h4>Computer Science & Engineering</h4>
          <p>Government Polytechnic College Khirsadoh<br>Final Year Batch 2022</p>
        </div>
        <div class="row">
          <div class="col-md-6">
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Ashish Sahu (22023C04010)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Akash Soni (22023C04011)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Aman Sahu (22023C04012)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Ankit Sahu (22023C04013)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Bhupendra Sahu (22023C04014)</span>
            </div>
          </div>
          <div class="col-md-6">
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Devendra Sahu (22023C04015)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Harsh Sahu (22023C04016)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Himanshu Sahu (22023C04017)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Khilesh Sahu (22023C04018)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Lokesh Sahu (22023C04019)</span>
            </div>
            <div class="team-member">
              <i class="fas fa-user-graduate"></i>
              <span>Yogesh Sahu (22023C04020)</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Contact Section -->
    <div class="about-card contact-section">
      <i class="fas fa-envelope contact-icon"></i>
      <h2>Get in Touch</h2>
      <p>Have questions or suggestions? We'd love to hear from you!</p>
      <p class="mt-3">
        <i class="fas fa-envelope-open-text mr-2"></i>
        support@notesvault.com
      </p>
    </div>
  </div>

  <!-- Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

  <!-- Custom Scripts -->
  <script>
    // Starfield Animation
    const canvas = document.getElementById('starfield');
    const ctx = canvas.getContext('2d');
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    let stars = Array.from({ length: 100 }).map(() => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      r: Math.random() * 1.5 + 0.5,
      dx: Math.random() * 0.5 + 0.2
    }));

    function animate() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = 'white';
      stars.forEach(star => {
        ctx.beginPath();
        ctx.arc(star.x, star.y, star.r, 0, 2 * Math.PI);
        ctx.fill();
        star.x -= star.dx;
        if (star.x < 0) {
          star.x = canvas.width;
          star.y = Math.random() * canvas.height;
        }
      });
      requestAnimationFrame(animate);
    }

    animate();

    // Team Section Toggle
    document.getElementById('teamHeader').addEventListener('click', function() {
      const content = document.getElementById('teamContent');
      const icon = this.querySelector('.rotate-icon');
      content.classList.toggle('active');
      icon.classList.toggle('active');
    });

    // Handle window resize
    window.addEventListener('resize', () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    });
  </script>
</body>
</html> 