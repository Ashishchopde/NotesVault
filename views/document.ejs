<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %> - NotesVault</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background: #000000;
      color: white;
      height: 100vh;
      overflow: hidden;
    }

    .pdf-container {
      width: 100%;
      height: 100vh;
      border: none;
    }

    .error-message {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: rgba(255, 255, 255, 0.1);
      padding: 20px;
      border-radius: 10px;
      backdrop-filter: blur(10px);
      text-align: center;
    }
  </style>
</head>
<body>
  <% if (isPdf) { %>
    <iframe src="<%= pdfLink %>" class="pdf-container" frameborder="0"></iframe>
  <% } else if (error) { %>
    <div class="error-message">
      <h3>Error</h3>
      <p><%= error %></p>
    </div>
  <% } %>
</body>
</html>
